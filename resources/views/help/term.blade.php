<x-app-layout :imageNavbar="true">
<section class="bg-white py-16">
<div class="max-w-4xl mx-auto px-6">

    <h1 class="text-3xl font-bold mb-6">{{ __('app.term.title') }}</h1>

    <p class="text-gray-500 mb-6">
        {{ __('app.term.intro') }}
    </p>

    <!-- 1 -->
    <h2 class="text-xl font-semibold mt-8 mb-2">{{ __('app.term.s1_title') }}</h2>
    <p class="text-gray-500">
        {{ __('app.term.s1_desc') }}
    </p>

    <!-- 2 -->
    <h2 class="text-xl font-semibold mt-8 mb-2">{{ __('app.term.s2_title') }}</h2>
    <p class="text-gray-500">
        {{ __('app.term.s2_desc') }}
    </p>

    <!-- 3 -->
    <h2 class="text-xl font-semibold mt-8 mb-2">{{ __('app.term.s3_title') }}</h2>
    <p class="text-gray-500">
        {{ __('app.term.s3_desc') }}
    </p>

    <!-- 4 -->
    <h2 class="text-xl font-semibold mt-8 mb-2">{{ __('app.term.s4_title') }}</h2>
    <p class="text-gray-500">
        {{ __('app.term.s4_desc') }}
    </p>

    <!-- 5 -->
    <h2 class="text-xl font-semibold mt-8 mb-2">{{ __('app.term.s5_title') }}</h2>
    <p class="text-gray-500">
        {{ __('app.term.s5_desc') }}
    </p>

    <!-- 6 -->
    <h2 class="text-xl font-semibold mt-8 mb-2">{{ __('app.term.s6_title') }}</h2>
    <p class="text-gray-500">
        {{ __('app.term.s6_desc') }}
    </p>

    <!-- 7 -->
    <h2 class="text-xl font-semibold mt-8 mb-2">{{ __('app.term.s7_title') }}</h2>
    <p class="text-gray-500">
        {{ __('app.term.s7_desc') }}
    </p>

    <!-- 8 -->
    <h2 class="text-xl font-semibold mt-8 mb-2">{{ __('app.term.s8_title') }}</h2>
    <p class="text-gray-500">
        {{ __('app.term.s8_desc') }}
    </p>

</div>
</section>
</x-app-layout>